---
title: Le Wagon - Middleman
---

<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,0,0,0.6) 0%, rgba(0,0,0,0.2) 100%), url('https://kitt.lewagon.com/placeholder/cities/paris');">
  <div class="banner-content">
    <h1>Home Welcome</h1>
    <p>Rent 190+ unique hosts from local countries in places to stay</p>
    <form action="" class="form-inline">
      <input type="text" class="form-control input-lg" placeholder="Checkin">
      <input type="text" class="form-control input-lg" placeholder="Checkout">
      <select class="form-control input-lg">
        <option value="1">1 person</option>
        <option value="2">2 people</option>
        <option value="3">3 people</option>
      </select>
      <a class="btn btn-primary btn-lg">Start now</a>
    </form>
  </div>
</div>

<div class="wrapper padded">

<div class="container text-center">
  <h2><strong>Recent flats</strong></h2>
  <div class="row">
    <% data.users.each do |user| %>
      <div class="col-xs-12 col-md-4">
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url(<%= user.picture %>);">
          <div class="card-category"><%= user.city %></div>
          <div class="card-description">
            <h2><%= user.name %>'s home</h2>
            <p><%= user.description %></p>
            <p class="rating">
              <% (user.rating).times do %>
                <i class="fa fa-star" style="color: #ffb400"></i>
              <% end %>
            </p>
          </div>
          <img class="card-user" src= <%= user.avatar %> >
          <a class="card-link" href="#" ></a>
        </div>
      </div>
    <% end %>
  </div>
</div>

